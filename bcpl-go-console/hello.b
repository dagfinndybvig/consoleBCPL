// This is reputedly one of the oldest Hello world programs in computing history
// To run it, just navigate to the relevant folder for your system and execute (Linux-style):
// ./compile.sh ../examples/hello.b
// The familiar greeting will appear in the console along with some other information

// Just note that if you want to use Rust you must build the interpreter first!
// The others should work out of the box

GET "LIBHDR"

LET START() BE WRITES("Hello, World!*N")


