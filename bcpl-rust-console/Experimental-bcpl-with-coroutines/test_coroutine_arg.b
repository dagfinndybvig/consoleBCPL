GET "LIBHDR"
GET "coroutines"

LET ARGCO(ARG) = VALOF
$( RESULTIS ARG
$)

LET START() BE
$( INITCO()
   LET C = CREATECO(ARGCO, 10)
   IF C=0 DO STOP(210)

   LET R = CALLCO(C, 1234)
   IF R NE 1234 DO STOP(211)

   WRITES("TEST PASSED")
   NEWLINE()

   DELETECO(C)
$)
